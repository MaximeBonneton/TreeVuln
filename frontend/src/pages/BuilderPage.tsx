import { TreeBuilder } from '@/components/TreeBuilder';

/** Page Builder : l'éditeur d'arbre existant, débarrassé du TreeSidebar (navigation dans le shell). */
export function BuilderPage() {
  return <TreeBuilder />;
}
